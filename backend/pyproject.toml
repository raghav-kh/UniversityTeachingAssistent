[project]
name = "eduai-backend"
version = "0.1.0"
requires-python = ">=3.11"
description = "EduAI FastAPI backend for Vercel serverless"

# Tells Vercel where the FastAPI `app` lives (main.py is not a default filename).
[project.scripts]
app = "main:app"
